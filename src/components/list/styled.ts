"use client";

import { Box, styled } from "@mui/material";

export const StyledTableContainer = styled(Box)(() => ({
  display: "flex",
  flexDirection: "column",
  flexGrow: 1,
  width: "100%",
  height: "100%",
  overflow: "hidden",
  position: "relative",
}));
